PASOS DEPLOY BACK-END:

1. Terminal:
   git clone https://github.com/valentingm1/spazio-BE.git

2. Agregar dentro de este archivo tu nombre completo y la rama que vas a utilizar:

3. Terminal:
   git init
   git git add .
   git commit -m "NombreCompleto, first commit"
   git branch -M main
   git remote add origin https://github.com/valentingm1/spazio-BE.git
   git push -u origin main

4. Luego de realizar el primer push, desde el IDE de preferencia y abrir el proyecto clonado.

5. Crear un archivo .env con la siguiente estructura dentro de la carpeta raiz del proyecto:
   ENV_USERNAME=admin
   ENV_PASSWORD=equipo03

6. Tambien, se debe agregar la variable de entorno en el IDE.
   Para intelliJ, EJEMPLO:

- Abre tu proyecto en IntelliJ IDEA.
- Ve al menú "Run" (Ejecutar) en la parte superior.
- Selecciona "Editar configuraciones".
- En la ventana de configuración que se abre, encontrarás la configuración de ejecución para tu aplicación Spring Boot.
- Dentro de la configuración de ejecución, busca la sección "Environment" (Entorno).
- En esta sección, verás una lista de variables de entorno definidas. Puedes agregar nuevas variables de entorno haciendo clic en el icono "+" o editar las existentes haciendo clic en ellas.
- Ingresa el nombre y el valor de la variable de entorno que deseas configurar.
- Haz clic en "OK" para guardar los cambios.

8. agregar el .env a .git

7. Por ultimo, abrir workbench y crear una nueva base de datos utilizandoo este script y ejecutar en workbench:
   Script SQL:

-- Crear la base de datos
CREATE DATABASE spazio_db;

-- Usar la base de datos
USE spazio_db;

8. Finalmente, actualizar los esquemas del workbench y validar si se cargaron las tablas "lugar" y "foto".
   En caso de ser asi, se configuro correctamente. Caso contrario comunicarse con alguno de los compañeros: Valentin o Ramiro

---

DATOS DE PROYECTO:
PROYECTO_INTEGRADOR:
NAME: "SPAZIO"
BASE_DE_DATOS: "MYSQL, RELACIONAL" URL: "database-spazio.ctka0aemsjdv.sa-east-1.rds.amazonaws.com", PASSWORD: "equipo03"
SPRING_BOOT_JAVA: "INICIADOR"

PARTICIPANTES SINCRONIZADOS EN GITHUB:
VALENTIN
RAMIRO ----------> branch: arceRamiro
